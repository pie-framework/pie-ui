import React, {Component} from 'react';

export default class TickUnit extends Component {

  constructor (props) {
    super (props);
  }

  render() {
    return(
      <div>Tick Unit</div>
    );
  }
}